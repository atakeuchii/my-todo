# 多言語学習プロジェクト リファレンス

*更新日: 2026-07-20*

## プロジェクト概要

学習したことのないプログラミング言語を、1言語あたり2週間で一通り理解する。
全言語で**共通のリファレンス課題(TODOアプリ)**を実装し、固定の比較軸で所感を書き残すことで、各言語の特徴を自分の言葉で語れるようにする。

- **既習（過去の同プロジェクト）**: Go, Java, TypeScript
- **本業**: Clojure（関数型・immutable・動的型・JVM）
- **今回の対象**: Ruby → Elixir → Rust → Scala
- **保留**: PHP（上記4つが終わった時点で必要だと感じたら実施）
- **ペース想定**: 1日1時間程度・14日/言語・バッファなし

## 目的

1. **言語間比較を通じた特徴理解**
   Clojureが関数型・immutableであることは知識としては持っているが、その利点がどこで生き・どんな欠点があるのかが腑に落ちていない。他言語を触ることで各言語の利点/欠点を自分なりに理解し、「なぜその言語を選ぶのか」を言語化できるようにする。
2. **言語の流れ（系譜）を追う**
   本業のClojureがJVM系列であるため、Javaのどの部分を使っているのか、Javaから派生した他言語がどんなものかを理解する。
3. **副業/転職時の選択肢を増やす**
   Clojureの求人は稀。backendで使われるRuby等を「触ったことがある状態」にして幅を広げる。

## 言語選定の理由

| 言語 | 主な貢献目的 | 学びのポイント |
|------|------|------|
| **Ruby** | ③（＋①） | 純粋OOP・ブロック・メタプログラミング/DSL。日本のRails文化圏で求人が厚い。参入障壁が低くウォームアップに向く。 |
| **Elixir** | ①（＋②の対照） | Clojureと同じ関数型・immutable。違いがランタイム/並行モデル（BEAM・プロセス・OTP・supervision・"let it crash"）に絞られ、Clojureを対照群にした綺麗な比較になる。 |
| **Rust** | ① | 所有権・借用という新しいメンタルモデル。GCなしのimmutable、`Option`/`Result`（null・例外がない）。Clojureの「immutable + GC + 動的」と対極で、immutableの意義を別角度から理解できる。 |
| **Scala** | ①②③すべて | Clojureと同じJVM上の言語。「Javaの上でLisp的に振ったのがClojure、静的型+FP/OOPに振ったのがScala」という系譜の対比。ADT・網羅的パターンマッチ・型駆動設計という、今回のセットで唯一の「静的型付き関数型」の学び。 |

### 補足メモ
- **PHP**: 動的OOP・mutableでRuby/TS経験者には新規性が薄く、Rubyと守備範囲が重複するため保留。残す理由は③のみ。
- **Kotlin（不採用）**: JVM系列理解（②）には直結するが、Javaを深く知る身には①の伸びが小さい（"better Java"）。今回はScalaに静的FPの学びを担わせる方針を優先。

## 題材選定の経緯

「並行データ収集・集約」を当初案として検討したが、モックソース準備の手間・ドメインへの愛着の薄さから見直し。
最終的に、目的①（言語そのものの理解）と目的③（HTTP/DB/フレームワーク経験が残る）のバランスから **TODOアプリを適度に複雑化した題材** に決定。

- **パーサ/インタプリタ**（目的①に最も刺さる）も候補だったが、Web要素ゼロで目的③から遠いため今回は見送り。
- **予約/在庫システム**は状態モデリングの学びは大きいが、興味が持てないため不採用。
- TODOは素だとCRUDに近く軽すぎるため、状態機械・サブタスク木構造・検索/集計・認証を乗せて難易度4前後に引き上げる。

## リファレンス課題: TODOアプリ

APIとして実装（フロントは作らない）。確認はcurl/テストで行う。

### エンティティ

`Task`: id, title, status, priority, deadline, tags[], parent_id, user_id, created_at, updated_at

### Must（言語比較の本体。全言語で必ず到達）

- Task のCRUD + バリデーション + 意味のあるエラーレスポンス
- **状態機械**（todo → in_progress → done。doneから戻せない等の不正遷移を拒否）
  → ADT/sealed型(Scala/Rust)で不正状態を型で防ぐか、実行時か、動的かの差が出る
- **サブタスク**（parent_idによる木構造。未完了の子があれば親を完了不可、サブツリーの再帰取得）
  → 各言語の再帰とデータ変換の書き味の比較
- **検索/フィルタ/ソート/ページネーション**（タグ・優先度・締切・状態）
  → 目的①の核心。Clojureのthreadingと直接比較できるイミュータブル変換の見せ場
- ユーザーごとのタスク分離（スコープの形は必須）

### Should（余裕があれば）

- **認証本体**（パスワードハッシュ + トークン/セッション + 検証ミドルウェア）
- **集計**（タグ別の残タスク数、期限切れ一覧、今週完了数）

### Optional（最初に削る）

- 一括操作 / 繰り返しタスク / タスク間依存

### 削り順（遅れた場合）

依存 → 繰り返し → 一括 → 集計 → **認証を簡易版へ降格**
（トークン検証を省き `user_id` をヘッダで受けるだけにして"分離"の形だけ残す）

各要素は独立しているため、後ろから落としても前半の言語比較の本体は崩れない。
**運用してみて序盤（ツールチェーン把握）が厳しければ、Shouldごと落とす判断でよい。**

## 14日スケジュール（1言語あたり・1日1時間）

| 日 | やること | 対応Tier |
|:--:|------|:--:|
| 1 | ツールチェーン把握・プロジェクト雛形・HTTPサーバ起動("hello"が返る) | 土台 |
| 2 | DB接続・tasksテーブルのマイグレーション・1件insertの疎通 | 土台 |
| 3 | Create + Read（単一GET・一覧GET） | Must |
| 4 | Update + Delete + バリデーション + エラーレスポンス | Must |
| 5 | 状態機械（不正遷移の拒否） | Must |
| 6 | サブタスク：parent_id・木構造・「未完了の子があれば親を完了不可」ルール | Must |
| 7 | サブタスク：サブツリーの再帰取得・再帰処理 | Must |
| 8 | 検索/フィルタ（タグ・優先度・締切・状態） | Must |
| 9 | ソート + ページネーション | Must |
| 10 | 認証①：ユーザーモデル・パスワードハッシュ・register/login | Should |
| 11 | 認証②：トークン/セッション発行 + 検証ミドルウェア | Should |
| 12 | ユーザーごとのタスク分離（認証をタスク系エンドポイントに結線） | Must/Should |
| 13 | 集計（タグ別残数・期限切れ一覧・今週完了数） | Should |
| 14 | 統合・エッジケースのテスト・仕上げ | 全体 |

- **中間チェック: 9日目終了時点**。Mustが埋まっていれば10日目以降のShouldへ安心して入れる。
- 総計14時間は初学言語のMust+Shouldとしては攻めた見積もり。特に1〜2日目のツールチェーン/DBライブラリ把握は1時間に収まらず溢れやすい。溢れたら削り順で後半を吸収する。

## 比較軸（各言語で振り返りメモを残す）

「言語化できるようにしたい」が目的なので、**この振り返りメモこそが成果物**。軸を固定して apples-to-apples の比較にする。
各言語ごとに、以下の軸で「何が楽だったか・何が痛かったか・何に驚いたか」を書く。

- 並行処理モデル
- エラーハンドリング
- 型・ドメインモデリング
- イミュータビリティとデータ変換
- ビルド・依存管理・ツールチェーン
- テストの書き味
- フレームワーク成熟度
- 学習曲線

## 想定フレームワーク

| 言語 | 代表的なWebフレームワーク |
|------|------|
| Ruby | Ruby on Rails / Sinatra |
| Elixir | Phoenix |
| Rust | Axum / Actix |
| Scala | Play / http4s |

※ 目的は「言語を学ぶ」ことなので、フレームワークに飲まれて「触っただけ」にならないよう、言語のイディオムに意識を向ける。

## 未決事項 / TODO

- [ ] DB・HTTPフレームワークを各言語で1つずつ確定（1言語目のRuby着手時に決める）
- [ ] PHPを追加するかの判断（4言語完了時点）
